import { UserDetailsResult } from './user-details-result';

describe('UserDetailsResult', () => {
  it('should create an instance', () => {
    expect(new UserDetailsResult()).toBeTruthy();
  });
});
